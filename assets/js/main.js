const getElement = (element) => document.querySelector(element)

getElement('#btnThem').onclick = () => {
    getElement('#btnCapNhat').style.display = 'onclick'
}

const getInfor = () => {
    const element = document.querySelectorAll()
}
